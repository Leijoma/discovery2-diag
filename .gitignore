__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
*.log
logs/*.csv
*.sqlite
*.sqlite3
data/
.DS_Store

# Car data dumps — never publish (may contain EKA/VIN)
logs/
captures/
*.raw.log

# Fuel trip-computer lifetime state (local)
fuel_totals.json

# ESP32 WiFi/OTA credentials — never publish (copy secrets.example.h → secrets.h)
secrets.h

# Private protocol-inventory source docs (contain EKA/VIN/ECU IDs, Swedish, tool names).
# The public knowledge base lives in docs/**/*.md; these .docx sources must never publish.
docs/*.docx
